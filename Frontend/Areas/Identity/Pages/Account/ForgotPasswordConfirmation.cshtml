@page
@model ForgotPasswordConfirmation
@{
    ViewData["Title"] = "Återställning skickad";
}

<h1>@ViewData["Title"]</h1>

@if (TempData["DevResetLink"] is string resetLink)
{
    <div class="alert alert-warning">
        <strong>Dev-läge:</strong> Återställ direkt via länken:<br />
        <a href="@resetLink" target="_blank" rel="noopener">Klicka här för att återställa lösenord</a>
    </div>
}

<p>
    Om e-postadressen finns i systemet har vi skickat en länk för att återställa ditt lösenord.
    Kontrollera din inkorg.
</p>
